# Copy to .env for docker compose and local tooling (`make config`).
PYTHONUNBUFFERED=1
# Local default; docker-compose.yml sets VERIDOC_CONFIG_DIR=/app/config for containers.
VERIDOC_CONFIG_DIR=config
